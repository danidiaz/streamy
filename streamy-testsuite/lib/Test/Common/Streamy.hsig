signature Test.Common.Streamy (
          Stream
        , yield
        , each
        , toList
        , toList_
        , chain
        , effects
        , concat
    ) where

import Prelude () -- weird confusions otherwise
